server.port=8080
spring.application.name=lab-turnos
jwt.secret=${JWT_SECRET:dev-secret-change}
jwt.expiration.minutes=120
spring.data.mongodb.uri=${MONGODB_URI:}
spring.data.mongodb.database=${MONGODB_DB:lab_turnos}